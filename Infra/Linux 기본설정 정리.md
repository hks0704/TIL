## 우분투 한글 입력
1. Region > Language 에서 한글 설치
2. 다음 명령어로 재부팅 : `reboot`
3. keyboard 탭에서 기존 English 제거 후 한글 추가
4. keyboard > 한글 > preference
5. 기존 토글 전부 제거
6. add 클릭 후 한/영 변환 키 입력

---

## 현재 경로 밑에 모든 파일 권한설정 일괄 적용
`sudo chmod -R 777 ./`
